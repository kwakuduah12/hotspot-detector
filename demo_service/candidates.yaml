# Shadow / candidate hotspots — identified, not wired.
# Copy an entry into hotspot-manifest.yaml once a workload exists.
# demo-platform export is live; this file is empty until the next area.
version: 1
service_area: demo-platform
hotspots: []
